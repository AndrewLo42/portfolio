version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: server/public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
